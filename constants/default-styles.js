import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    bodyText: {
        fontFamily: 'open-sans'
    },
    title: {
        fontFamily: 'open-sans-bold',
        fontSize: 18
    }
});

//can create constants that are globally managed for styles like this or alternatively set up custom components for different font styles. 
